To run this program. Open a termnial and use command [make] to compile, the executable is named as [graph.cpp]

run the executable binary, then input the name of the file you want to parse in to the program.
input file need to be in strict format of "int string string string int" in every line.

program output will be displayed in termnial.